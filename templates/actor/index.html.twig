{% extends 'base.html.twig' %}

{% block title %} Tous les acteurs{% endblock %}

{% block body %}
        <h1> Tous les acteurs </h1>
        <ul>
                {% for actor in actors %}
                        <li>
                                {{ actor.name }}
                                <a href="{{ path('actor_show',{'id':actor.id}) }}">Fiche de l'acteur</a>
                        </li>
                {% endfor %}
        </ul>
{% endblock %}

